/**
 * WS4 (Nurzat) owns this file.
 *
 * Placeholder so `/decks` has something to render and App.jsx never needs a second
 * edit — see "Shared files" in ITERATION_1_PLAN.md. Replace the whole body; the only
 * thing WS3 depends on is the default export and the filename.
 */

export default function DeckListPage() {
  return (
    <main className="page">
      <h1>Your decks</h1>
      <p className="page-status">Coming in WS4.</p>
    </main>
  );
}
