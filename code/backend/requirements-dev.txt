# Tooling that CI and local development need, but the deployed image does not.
# The Dockerfile installs requirements.txt only, so none of this ships to production.
#
#     pip install -r requirements.txt -r requirements-dev.txt

ruff==0.14.0
pytest-cov==7.0.0
